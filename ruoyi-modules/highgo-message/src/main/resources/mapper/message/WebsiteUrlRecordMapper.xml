<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.highgo.message.mapper.WebsiteUrlRecordMapper">

    <resultMap type="WebsiteUrlRecord" id="WebsiteUrlRecordResult">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="logo" column="logo"/>
        <result property="title" column="title"/>
        <result property="url" column="url"/>
        <result property="groupName" column="group_name"/>
        <result property="remark" column="remark"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectWebsiteUrlRecordVo">
        select id,
               name,
               logo,
               title,
               url,
               group_name,
               remark,
               status,
               create_by,
               create_time,
               update_by,
               update_time
        from website_url_record
    </sql>

    <select id="selectWebsiteUrlRecordList" parameterType="WebsiteUrlRecord" resultMap="WebsiteUrlRecordResult">
        <include refid="selectWebsiteUrlRecordVo"/>
        <where>
            <if test="name != null and name != ''">and name like concat('%', #{name}, '%')</if>
            <if test="url != null and url != ''">and url like concat('%', #{url}, '%')</if>
            <if test="groupName != null and groupName != ''">and group_name like concat('%', #{groupName}, '%')</if>
            <if test="status != null and status != ''">and status = #{status}</if>
            <if test="createBy != null and createBy != ''">and create_by = #{createBy}</if>
        </where>
    </select>

    <select id="selectWebsiteUrlRecordById" parameterType="Long" resultMap="WebsiteUrlRecordResult">
        <include refid="selectWebsiteUrlRecordVo"/>
        where id = #{id}
    </select>

    <insert id="insertWebsiteUrlRecord" parameterType="WebsiteUrlRecord" useGeneratedKeys="true" keyProperty="id">
        insert into website_url_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="logo != null">logo,</if>
            <if test="title != null">title,</if>
            <if test="url != null and url != ''">url,</if>
            <if test="groupName != null">group_name,</if>
            <if test="remark != null">remark,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name},</if>
            <if test="logo != null">#{logo},</if>
            <if test="title != null">#{title},</if>
            <if test="url != null and url != ''">#{url},</if>
            <if test="groupName != null">#{groupName},</if>
            <if test="remark != null">#{remark},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <update id="updateWebsiteUrlRecord" parameterType="WebsiteUrlRecord">
        update website_url_record
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null">name = #{name},</if>
            <if test="logo != null">logo = #{logo},</if>
            <if test="title != null">title = #{title},</if>
            <if test="url != null and url != ''">url = #{url},</if>
            <if test="groupName != null">group_name = #{groupName},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteWebsiteUrlRecordById" parameterType="Long">
        delete
        from website_url_record
        where id = #{id}
    </delete>

    <delete id="deleteWebsiteUrlRecordByIds" parameterType="String">
        delete from website_url_record where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>